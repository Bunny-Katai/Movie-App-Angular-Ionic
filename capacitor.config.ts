import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'movieAppTabs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
